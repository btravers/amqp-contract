/**
 * Priority Queue Example
 *
 * This example demonstrates priority queue processing with amqp-contract.
 *
 * Run the client (publisher):
 *   pnpm start:client
 *
 * Run the worker (consumer):
 *   pnpm start:worker
 */

export { priorityQueueContract } from "./contract.js";
