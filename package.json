{
  "name": "@amqp-contract/root",
  "private": true,
  "description": "Type-safe contract system for AMQP architecture",
  "license": "MIT",
  "author": "Benoit TRAVERS <benoit.travers.fr@gmail.com>",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "build:docs": "turbo run build:docs",
    "changeset": "changeset",
    "dev": "turbo run dev",
    "docs:build": "pnpm --filter @amqp-contract/docs docs:build",
    "docs:dev": "pnpm --filter @amqp-contract/docs docs:dev",
    "docs:preview": "pnpm --filter @amqp-contract/docs docs:preview",
    "format": "oxfmt .",
    "lint": "oxlint .",
    "prepare": "lefthook install",
    "release": "pnpm build && changeset publish",
    "setup": "pnpm install && pnpm build",
    "sort-package-json": "sort-package-json '**/package.json' '!**/node_modules/**'",
    "test": "turbo run test",
    "test:integration": "turbo run test:integration",
    "typecheck": "turbo run typecheck",
    "version": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "knip": "catalog:",
    "lefthook": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "sort-package-json": "catalog:",
    "turbo": "catalog:"
  },
  "packageManager": "pnpm@10.26.2"
}
